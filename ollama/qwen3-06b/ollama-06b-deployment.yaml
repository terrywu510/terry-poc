apiVersion: apps/v1
kind: Deployment
metadata:
  name: ollama-06b
  namespace: ollama
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ollama-06b
  template:
    metadata:
      labels:
        app: ollama-06b
    spec:
      containers:
      - name: ollama
        image: ghcr.io/terrywu510/model:ollama-qwen3-06b
        ports:
        - containerPort: 11434
        env:
        - name: OLLAMA_HOST
          value: "0.0.0.0"
        resources:
          requests:
            memory: "30Gi"
            cpu: "8"
            otterscale.com/vgpu: "1"
            otterscale.com/vgpumem-percentage: "10"
          limits:
            memory: "30Gi"
            cpu: "8"
            otterscale.com/vgpu: "1"
            otterscale.com/vgpumem-percentage: "10"
---
apiVersion: v1
kind: Service
metadata:
  name: ollama-06b
  namespace: ollama
spec:
  type: NodePort
  selector:
    app: ollama-06b
  ports:
    - port: 80
      targetPort: 11434
      nodePort: 30007